locals {
  bucket_name                   = "${var.env}-reclada-bucket"
  # cluster_name                  = "eks-infra-cluster"
  # k8s_service_account_namespace = "kube-system"
  # k8s_service_account_name      = "cluster-autoscaler-aws-cluster-autoscaler-chart"
}
