apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "sshfs.serviceAccountName" . }}
  labels:
    {{- include "sshfs.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "sshfs.serviceAccountName" . }}
subjects:
- kind: ServiceAccount
  name: {{ include "sshfs.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
