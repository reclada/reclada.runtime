apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.coordinator.volume2.pvcname }}
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: efs-sc
  resources:
    requests:
      storage: {{ .Values.coordinator.volume2.size }}